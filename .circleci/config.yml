version: 2.1



jobs:
  dockerfun:
    docker:
      - image: ubuntu:18.04
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: |
            apt-get update -qq -y
            apt-get install -qq -y
            apt-get -y install sudo
            sudo ./update.sh
            cat deployment.yml





workflows:
  default:
    jobs:
      - dockerfun





